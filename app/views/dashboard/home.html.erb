<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Slow Poker</title>
</head>
<body>
  <div class="row">
    <div class="col-md-12">
      <table class="table table-striped table-hover">
        <% if current_user != nil %>
        <tr>
          <th>Table</th>
          <th>Players</th>
          <th>Big Blind</th>
          <th>Whose turn?</th>
          <th>Actions</th>
          <th>Table ID</th>
          <th>Table password</th>
        </tr>

        <% @table.each do |table| %>
        <tr>
          <td><%= table.name %></td>
          <td>poopoo</td>
          <td><%= table.big_blind %></td>
          <td><%= table.active_player %></td>

          <td>
            <a href="/" class="btn btn-primary">Open</a>
            <a href="/leave_table/<%= table.id %>" class="btn btn-warning">Leave</a>
            <a href="/" class="btn btn-danger" rel="nofollow">Edit</a> <!-- if authorized -->
          </td>
          <td><%= table.id %></td>
          <td><%= table.password %></td>
        </tr>
        <% end %>
        <% end %>
      </table>
    </div>
  </div>
</body>
</html>
